# Rust build artifacts
/target

# Release tarballs + checksums (make release)
/dist

# Local build symlink (regenerated by `make link` / `make install`)
/bin

# speckit local state (regenerated by `speckit` tooling)
/.specify

# macOS finder metadata
.DS_Store
**/.DS_Store

# Editor / local scratch
*.swp
*~
